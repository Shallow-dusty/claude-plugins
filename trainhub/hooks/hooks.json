{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "for f in $CLAUDE_FILE_PATHS; do case \"$f\" in */weights/*|*/runs/*|*/results/*|*/datasets/*|*/checkpoints/*) echo \"BLOCK: trainhub 保护 — 禁止直接修改 weights/ runs/ results/ datasets/ checkpoints/ 下的文件。这些是训练产物，应由训练脚本写入。\" >&2; exit 2;; esac; done; exit 0"
          }
        ]
      }
    ]
  }
}
