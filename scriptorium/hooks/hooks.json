{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "for f in $CLAUDE_FILE_PATHS; do if [[ \"$f\" == *.py ]]; then ruff format --quiet \"$f\" 2>/dev/null; ruff check --fix --quiet \"$f\" 2>/dev/null; fi; done; exit 0"
          }
        ]
      }
    ]
  }
}
